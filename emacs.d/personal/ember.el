


;; (defun hbs-goto-component ()
;;  "Loads the component.js file for a component in {{handle-bars }}"

;;  (search-backward "{{")
;;  (ty-forward-symbol nil)

;;   ;; look for a symbol
;;  (re-search-forward "[\.\\:'@a-zA-Z0-9]['^@a-zA-Z0-9_\\/:-]*[a-zA-Z0-9]*")

;;  (let ((comp-name (buffer-substring (match-beginning 0) (match-end 0) )))
;;    (find-file "init.exl")
;;    ) 
;; )
